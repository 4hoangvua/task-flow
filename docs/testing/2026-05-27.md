# Nhật ký Kiểm thử (Testing Log) – 2026-05-27

Bản ghi kết quả kiểm thử cho các tính năng:
1. **Kiểm thử Chức năng (Manual Functional Testing)**: Trình soạn thảo Markdown, Slash Commands, Trợ lý AI Soạn thảo, và Lịch công việc dự án.
2. **Kiểm thử Tự động (Automated Testing)**: Unit Test cho Frontend (Vitest + React Testing Library) và Integration Test cho Backend (Vitest + Supertest).

---

## I. KIỂM THỬ CHỨC NĂNG (MANUAL FUNCTIONAL TEST CASES)

### [x] Soạn thảo Markdown với thanh công cụ (Markdown Editor Toolbar)
- **Kịch bản**: 
  1. Mở modal "Tạo nhiệm vụ mới".
  2. Bôi đen văn bản và click vào các nút công cụ: Bold (In đậm), Italic (In nghiêng), Link (Liên kết), Checklist (Hộp kiểm), Code (Khối mã), H1, H2.
  3. Chuyển sang Tab "Xem trước" để kiểm tra hiển thị.
- **Trạng thái**: PASS (Hệ thống tự động chèn đúng các cặp ký tự cú pháp tương ứng `**`, `*`, `[ ]`, v.v. tại vị trí con trỏ. Chế độ Xem trước hiển thị đúng các thẻ HTML tương đương).

### [x] Phím tắt Lệnh nhanh bằng dấu gạch chéo (Slash Commands `/`)
- **Kịch bản**: 
  1. Trong ô soạn thảo mô tả, gõ dấu `/`.
  2. Xác nhận một menu popover hiển thị danh sách các cấu trúc Markdown (H1, H2, Bold, Checklist, Code Block).
  3. Nhấp chọn "Hộp kiểm (Checklist)" từ menu.
- **Trạng thái**: PASS (Menu popover mở ra đúng vị trí. Khi chọn lệnh, ký tự `/` được thay thế bằng `- [ ] ` và tiêu điểm quay lại ô soạn thảo với con trỏ ở vị trí chính xác).

### [x] Trợ lý AI Soạn thảo (Gemini 1.5 Flash Integration)
- **Kịch bản**:
  1. Nhập tiêu đề công việc: `"Xây dựng tính năng đăng nhập SSO Google"`.
  2. Click vào nút 🤖 **"Trợ lý AI Soạn thảo"** ở góc phải thanh công cụ.
  3. Chọn không nhập prompt bổ sung và nhấn **"Bắt đầu tạo"**.
- **Trạng thái**: PASS (Hệ thống gửi prompt thành công sang Gemini API qua endpoint `gemini-flash-latest`. Phản hồi trả về sau 1.2s là văn bản Markdown cấu trúc sạch đẹp với đầy đủ mục tiêu, các bước thực hiện checklist, tiêu chí nghiệm thu và tự động điền vào khung soạn thảo).

### [x] Rời/Hủy hoặc Soạn thảo bằng Prompt tùy chỉnh (Custom AI Prompts)
- **Kịch bản**:
  1. Click vào **"Trợ lý AI Soạn thảo"**.
  2. Nhập prompt tùy chỉnh: `"Viết mô tả ngắn gọn về việc tối ưu hóa hiệu năng câu lệnh SELECT SQL"`.
  3. Click **"Bắt đầu tạo"**.
- **Trạng thái**: PASS (AI soạn thảo đúng chủ đề yêu cầu thay vì soạn theo tiêu đề công việc mặc định).

### [x] Hiển thị Rich Markdown tại trang Chi tiết Công việc (Markdown Rendering)
- **Kịch bản**: 
  1. Lưu công việc vừa tạo.
  2. Click vào thẻ công việc đó trên Kanban Board để mở Modal chi tiết.
  3. Kiểm tra hiển thị phần mô tả công việc.
- **Trạng thái**: PASS (Trình `MarkdownRenderer` xử lý đúng toàn bộ tiêu đề h1/h2/h3, in đậm, in nghiêng, và khối mã code blocks được highlight trên nền tối obsidian gọn gàng).

### [x] Hộp kiểm (Checklist) mô tả công việc tương tác trực tiếp
- **Kịch bản**:
  1. Mô tả công việc chứa danh sách checklist Markdown: `- [ ] Bước 1`.
  2. Nhấp trực tiếp vào ô checkbox của dòng `Bước 1` khi đang ở chế độ xem chi tiết của task (không mở modal Edit).
  3. Kiểm tra xem checkbox có đổi sang trạng thái tích chọn `- [x]` và gửi API cập nhật database thành công không.
- **Trạng thái**: PASS (Checkbox thay đổi trạng thái ngay lập tức trên UI, gửi API `PUT /api/tasks/:id` lưu lại chuỗi mô tả cập nhật thành công, đồng bộ dữ liệu hoàn hảo).

### [x] Trình thu gọn mô tả và nút "Xem chi tiết" (Collapsible Description)
- **Kịch bản**:
  1. Tạo công việc có mô tả dài (hơn 250 ký tự).
  2. Mở Modal chi tiết nhiệm vụ và kiểm tra chiều cao của phần mô tả.
  3. Xác nhận phần mô tả bị giới hạn chiều cao (max-height), hiển thị lớp phủ mờ (fade-out) và xuất hiện nút "Xem chi tiết".
  4. Nhấp vào "Xem chi tiết" -> Mô tả mở rộng đầy đủ và nút chuyển thành "Thu gọn".
- **Trạng thái**: PASS (Hoạt động chính xác giúp Modal giữ được kích thước cân đối trên cả Desktop và di động).

### [x] Lọc Lịch công việc (Calendar Filters)
- **Kịch bản**:
  1. Chọn thành viên thực hiện trong Select "Chọn người thực hiện".
  2. Chọn độ ưu tiên "Khẩn cấp" hoặc "Cao" trong Select "Độ ưu tiên".
- **Trạng thái**: PASS (Hệ thống lọc chính xác các task badge hiển thị trên lịch, loại bỏ các task không khớp với tiêu chí lựa chọn).

### [x] Điều hướng tháng (Month Navigation)
- **Kịch bản**:
  1. Nhấn nút mũi tên trái `<` và phải `>`.
  2. Nhấn nút "Hôm nay".
- **Trạng thái**: PASS (Tháng hiển thị cập nhật chính xác, lưới lịch 42 ngày chuyển đổi mượt mà theo đúng tháng lựa chọn. Nút "Hôm nay" quay về tháng hiện hành ngay lập tức).

### [x] Mở Chi tiết Công việc từ Lịch (Open Detail Modal from Calendar)
- **Kịch bản**: Nhấp chuột vào một badge công việc (ví dụ: task "Xây dựng tính năng SSO Google") trên lưới lịch.
- **Trạng thái**: PASS (Modal chi tiết công việc `TaskDetailModal` hiển thị chính xác với đầy đủ thông tin mô tả Markdown, bình luận và lịch sử hoạt động).

### [x] Tạo Công việc nhanh với ngày chọn sẵn (Create Task from Day Cell)
- **Kịch bản**: 
  1. Đăng nhập với tài khoản Project LEADER. Rê chuột vào ngày 28/05/2026 trên lịch, click vào nút "+" hiển thị ở góc ô ngày.
  2. Đăng nhập với tài khoản MEMBER, rê chuột vào các ngày trên lịch.
- **Trạng thái**: PASS (Với tài khoản LEADER, modal `TaskFormModal` mở ra và điền sẵn hạn chót là `2026-05-28 17:00`. Với tài khoản MEMBER, nút "+" hoàn toàn bị ẩn và việc click vào ô ngày bị chặn, đáp ứng đúng phân quyền RBAC).

### [x] Đồng bộ hóa thời gian thực (Real-time Sync)
- **Kịch bản**: Sửa đổi deadline của một công việc trên modal chi tiết hoặc tạo mới công việc từ tab khác.
- **Trạng thái**: PASS (Sau khi lưu, nhờ cơ chế Socket.io room sync, tab Lịch công việc tự động cập nhật và hiển thị badge ở đúng ô ngày mới mà không cần tải lại trang).

### [x] Chế độ xem Lịch di động (Mobile Day-Grouped List View)
- **Kịch bản**: Co dãn chiều rộng trình duyệt xuống mức di động (< 768px) on tab Lịch công việc.
- **Trạng thái**: PASS (Giao diện tự động chuyển đổi sang dạng danh sách các ngày trong tháng có chứa task rất dễ đọc. Bảng stats chuyển thành thanh ngang hỗ trợ vuốt cuộn mượt mà. Nút tạo nhanh "+" ở tiêu đề ngày (Leader) và click card mở chi tiết task hoạt động tốt).

### [x] Tự kết nối lại và Rejoin phòng dự án (Socket Reconnect Room Rejoin)
- **Kịch bản**: 
  1. Mở trang Chi tiết dự án, ngắt kết nối mạng mô phỏng (gọi `socket.disconnect()` trực tiếp).
  2. Bật lại kết nối mạng mô phỏng (gọi `socket.connect()`).
  3. Thực hiện thay đổi task ở tab/trình duyệt khác.
- **Trạng thái**: PASS (Sau khi kết nối lại thành công, client tự động kích hoạt bộ lắng nghe `connect` và emit lại `join-project` để gia nhập phòng dự án. Các thay đổi task từ tab khác lập tức được cập nhật trên Kanban/Lịch thời gian thực mà không cần tải lại trang).

### [x] Tự làm mới Access Token khi Socket lỗi xác thực (Socket Auth Auto-Recovery)
- **Kịch bản**:
  1. Thay đổi Access Token thành token đã hết hạn hoặc không hợp lệ.
  2. Bắt đầu kết nối Socket.io client. Server từ chối kết nối và trả về lỗi `connect_error`.
- **Trạng thái**: PASS (Client bắt được lỗi `connect_error` chứa `'Authentication error'`, tự động gọi API `/api/auth/refresh` bằng Refresh Token thành công, nhận Access Token mới, cập nhật `authStore` và tiến hành kết nối Socket lại thành công hoàn toàn tự động).

---

## II. KIỂM THỰC TỰ ĐỘNG (AUTOMATED TEST SUITES)

### 1. Backend Integration Tests (Vitest + Supertest)
- **Lệnh chạy**: `npx.cmd vitest run` tại thư mục `backend/`
- **Cấu hình môi trường**: Chạy tuần tự trên SQLite Database biệt lập (`prisma/test.db`), tự động chạy push schema khởi tạo trước test suite và dọn dẹp file db sau test suite. Đã cấu hình `include: ['src/tests/**/*.test.ts']` để loại trừ các tệp CommonJS từ thư mục `dist/`. Đã tối ưu hóa hàm dọn dẹp dữ liệu `beforeEach` ở cả 3 tệp test để tránh xung đột khóa ngoại SQLite.
- **Kết quả**:
  * **File `auth.test.ts`**:
    - [x] Đăng ký thành viên mới (default role MEMBER) -> PASS
    - [x] Chặn đăng ký trùng email -> PASS
    - [x] Đăng nhập nhận Access & Refresh Token -> PASS
    - [x] Lấy thông tin cá nhân (Profile) -> PASS
    - [x] Cập nhật thông tin cá nhân -> PASS
  * **File `project.test.ts`**:
    - [x] Leader tạo dự án -> PASS
    - [x] Member tạo dự án cá nhân tự sở hữu -> PASS
    - [x] Project Owner sửa thông tin dự án -> PASS
    - [x] Chặn user không phải Owner sửa dự án -> PASS
    - [x] Project Owner quản lý thành viên (Thêm thành viên, sửa vai trò thành LEADER) -> PASS
  * **File `task.test.ts`**:
    - [x] Leader tạo task thành công -> PASS
    - [x] Chặn người ngoài xem công việc dự án -> PASS
    - [x] Task Dependencies: Chặn DONE khi prerequisite chưa hoàn thành -> PASS
    - [x] Chặn liên kết phụ thuộc tuần hoàn (Circular Dependencies) trực tiếp & gián tiếp bằng DFS -> PASS
    - [x] Quản lý Subtasks (Tạo, Toggle trạng thái, Xóa) -> PASS
- **Trạng thái chung**: **PASS 100% (15/15 tests)**

### 2. Frontend Unit & Component Tests (Vitest + React Testing Library)
- **Lệnh chạy**: `npm run test` tại thư mục `frontend/`
- **Môi trường**: jsdom, mock socket.io-client, mock Recharts, mock matchMedia.
- **Kết quả**:
  * **File `PriorityTag.test.tsx`**:
    - [x] Render chính xác nhãn tiếng Việt và class màu cho các độ ưu tiên (LOW, MEDIUM, HIGH, URGENT) -> PASS
  * **File `MarkdownRenderer.test.tsx`**:
    - [x] Hiển thị placeholder khi content trống -> PASS
    - [x] Render heading h1/h2 với regex match -> PASS
    - [x] Render text định dạng bold, italic -> PASS
    - [x] Gom nhóm lists liên tiếp vào một `<ul>` duy nhất với class `space-y-1` -> PASS
    - [x] Render checklist và click checkbox gọi callback `onCheckboxChange` tương ứng -> PASS
  * **File `TaskCalendar.test.tsx`**:
    - [x] Render header và các bộ lọc chọn người thực hiện, độ ưu tiên -> PASS
    - [x] Render các task badge hiển thị trên ô lịch -> PASS
    - [x] Click vào task badge mở modal chi tiết `TaskDetailModal` -> PASS
- **Trạng thái chung**: **PASS 100% (12/12 tests)**

---

## III. KIỂM TRA BIÊN DỊCH VÀ KIỂU DỮ LIỆU (COMPILE CHECKS)
- **Kịch bản**: Chạy kiểm tra kiểu dữ liệu tĩnh (`npx tsc`) trong `backend/` và (`npx tsc -b`) trong `frontend/`.
- **Trạng thái**: PASS (Cả frontend và backend đều biên dịch thành công 100% không phát sinh lỗi cảnh báo hoặc lỗi kiểu dữ liệu TypeScript nào).
