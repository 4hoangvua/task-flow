# Nhật ký Kiểm thử (Testing Log) – 2026-05-28

Bản ghi kết quả kiểm thử chức năng và kiểm thử tự động sau khi rà soát bug, sửa lỗi bảo mật & cải tiến giao diện.

---

## I. KIỂM THỰC CHỨC NĂNG (MANUAL FUNCTIONAL TEST CASES)

### [x] Kiểm tra khóa tài khoản tức thì (User Deactivation check)
* **Kịch bản**: 
  1. Đăng nhập với tài khoản `admin@taskflow.com`. Mở Admin Dashboard, khóa một tài khoản member (chuyển `isActive` sang `false`).
  2. Dùng tài khoản member đó gọi các API bằng access token cũ chưa hết hạn.
  3. Thử kết nối WebSocket mới bằng token cũ của member đó.
* **Kết quả**: 
  * API trả về mã lỗi `401 Unauthorized` với thông báo `"User account is inactive or not found"`.
  * Kết nối WebSocket bị từ chối với lỗi `'Authentication error: User account is inactive or not found'`.
* **Trạng thái**: PASS

### [x] Kiểm tra unassign công việc khi thành viên bị xóa khỏi dự án
* **Kịch bản**:
  1. Gán một số công việc của dự án cho thành viên A.
  2. Trưởng nhóm xóa thành viên A ra khỏi dự án.
  3. Kiểm tra xem các công việc từng được gán cho thành viên A trong dự án đó có tự động chuyển người thực hiện sang `"Chưa gán"` (`assigneeId` thành `null`) trong DB hay không.
  4. Xác nhận giao diện Kanban Board của các thành viên khác lập tức cập nhật lại (không còn hiển thị thành viên A).
* **Kết quả**:
  * Các task liên quan tự động chuyển sang trạng thái chưa gán trong cùng một transaction.
  * Sự kiện socket `member:removed` kích hoạt invalidate query `tasks`, làm mới giao diện Kanban lập tức trên các client khác mà không cần load lại trang.
* **Trạng thái**: PASS

### [x] Ràng buộc hạn chót không ở quá khứ khi cập nhật Task
* **Kịch bản**:
  1. Mở modal chỉnh sửa công việc.
  2. Thử nhập ngày hạn chót trong quá khứ và nhấn cập nhật.
  3. Kiểm tra xem backend và frontend có chặn hành động này không.
* **Kết quả**: 
  * Frontend chặn nhập qua DatePicker (vô hiệu hóa các ngày trong quá khứ).
  * API PATCH trả về lỗi validation `400` nếu cố tình gửi request payload chứa hạn chót ở quá khứ.
* **Trạng thái**: PASS

### [x] Hiển thị chi tiết lỗi nghiệp vụ thay vì lỗi Axios chung chung
* **Kịch bản**:
  1. Đổi trạng thái một task có dependency chưa hoàn tất sang `DONE`.
  2. Xác nhận xem thông báo lỗi hiển thị trên UI có ghi rõ tiêu đề của công việc tiên quyết chưa hoàn thành hay không.
* **Kết quả**: 
  * UI hiển thị chính xác: `"Không thể hoàn thành công việc vì các công việc tiên quyết chưa hoàn thành: "Task B""`.
* **Trạng thái**: PASS

### [x] Chặn chuyển trạng thái DONE trên UI trước khi gọi API (Kanban, MyTasks, TaskDetailModal)
* **Kịch bản**:
  1. Kéo thả một task có dependency chưa hoàn tất sang cột `DONE` trên Kanban.
  2. Chọn `DONE` ở dropdown status của task có dependency chưa hoàn tất trên trang MyTasks và TaskDetailModal.
* **Kết quả**:
  * Giao diện hiển thị lỗi và giữ nguyên vị trí/trạng thái cũ của task ngay lập tức mà không gặp tình trạng card nhảy sang DONE rồi giật lại (không nhấp nháy UI).
* **Trạng thái**: PASS

### [x] Tối ưu hóa chọn dự án thông minh ở Dashboard
* **Kịch bản**:
  1. Người dùng đang ở trang Dashboard, xem stats của dự án A.
  2. Ở trình duyệt khác, Trưởng nhóm xóa dự án A hoặc xóa người dùng đó khỏi dự án A.
  3. Quay lại trình duyệt cũ, kiểm tra xem Dashboard có tự động chuyển stats sang dự án tiếp theo hoặc hiển thị màn hình trống thân thiện thay vì crash/báo lỗi API 404 không.
* **Kết quả**: 
  * `selectedProjectId` tự động cập nhật về dự án hợp lệ khác hoặc `null`, không gọi API stats dự án cũ, giao diện hiển thị trạng thái Empty sạch sẽ.
* **Trạng thái**: PASS

### [x] Kiểm tra Cấu hình trợ lý AI cá nhân (Gemini API Key cá nhân)
* **Kịch bản**:
  1. Truy cập trang Cấu hình tài khoản (Settings), nhập một API Key không hợp lệ vào phần "Cấu hình trợ lý AI cá nhân".
  2. Bấm "Kiểm tra kết nối". Xác nhận thông báo kết nối thất bại xuất hiện.
  3. Nhập một API Key Gemini hợp lệ, bấm "Kiểm tra kết nối". Xác nhận thông báo kết nối thành công.
  4. Bấm "Lưu cấu hình". Refresh trang và kiểm tra xem API Key có được lưu và hiển thị lại không.
  5. Mở một công việc, click "Trợ lý AI Soạn thảo", gõ prompt và bấm "Bắt đầu tạo". Xác nhận nội dung Markdown được sinh ra thành công từ key cá nhân đó.
  6. Quay lại Cài đặt, bấm "Xóa cấu hình". Mở lại "Trợ lý AI Soạn thảo", xác nhận hệ thống hiển thị thông báo lỗi yêu cầu cấu hình API Key.
* **Kết quả**:
  * Kiểm tra kết nối với key sai trả về lỗi API chi tiết từ Google.
  * Kiểm tra kết nối với key đúng trả về thành công.
  * API Key lưu chính xác vào `localStorage`. Khi xóa cấu hình, key bị gỡ bỏ lập tức.
  * `MarkdownEditor.tsx` chặn sử dụng AI và hiển thị thông báo lỗi bằng `message.error` nếu không có key trong `localStorage`. Khi có key, việc gọi AI và tạo Markdown diễn ra bình thường.
* **Trạng thái**: PASS

### [x] Kiểm tra Bình luận phản hồi lồng nhau (Threaded Comments & Replies)
* **Kịch bản**:
  1. Đăng nhập và mở Modal chi tiết công việc.
  2. Viết một bình luận gốc (Root comment) mới. Xác nhận bình luận hiển thị chính xác.
  3. Click nút "Trả lời" (Reply) bên dưới bình luận đó, viết nội dung phản hồi con và gửi.
  4. Xác nhận phản hồi con hiển thị thụt lề dưới bình luận gốc, có đường kẻ dọc chỉ hướng.
  5. Viết thêm 2 phản hồi con khác để tổng số phản hồi là 3. Xác nhận chỉ có 2 phản hồi đầu tiên hiển thị, và xuất hiện nút *"Xem thêm 1 phản hồi..."*.
  6. Click nút *"Xem thêm..."*, xác nhận mở rộng đầy đủ cả 3 phản hồi và nút đổi thành *"Thu gọn phản hồi"*.
  7. Bấm phản hồi một comment con, xác nhận backend tự động làm phẳng (flatten) và đưa nó về làm con của bình luận gốc (giữ cấu trúc tối đa 2 cấp thân thiện mobile).
* **Kết quả**:
  * Bình luận gốc và các phản hồi hiển thị đúng cấu trúc lồng nhau.
  * Khoảng đệm thụt lề `ml-6 sm:ml-8` đảm bảo không bị hẹp màn hình trên mobile.
  * Cơ chế ẩn/hiện và mở rộng (Expand/Collapse) hoạt động đúng như mong đợi khi có nhiều phản hồi con.
  * Tự động làm phẳng reply lồng sâu về comment gốc thành công.
* **Trạng thái**: PASS

### [x] Kiểm tra chặn người dùng tự trả lời bình luận của chính mình
* **Kịch bản**:
  1. Đăng nhập, mở Modal chi tiết công việc.
  2. Viết một bình luận gốc. Kiểm tra xem nút "Trả lời" có hiển thị bên dưới bình luận đó hay không.
  3. Thử gửi một POST request trực tiếp tới API tạo bình luận (`/api/tasks/:id/comments`) với `parentId` trỏ đến ID của bình luận do chính mình tạo ra.
* **Kết quả**:
  * Giao diện ẩn hoàn toàn nút "Trả lời" dưới bình luận do chính người dùng đăng nhập tạo ra (nhưng vẫn hiển thị nút "Trả lời" dưới các bình luận của người khác).
  * Backend trả về lỗi `400 VALIDATION_ERROR` với thông báo `"Bạn không thể tự trả lời bình luận của chính mình"` và chặn tạo bản ghi trong DB.
* **Trạng thái**: PASS

### [x] Kiểm tra đồng bộ bình luận thời gian thực qua WebSockets (Real-time Comments Sync check)
* **Kịch bản**:
  1. Người dùng A và Người dùng B cùng mở Modal chi tiết công việc của Task A.
  2. Người dùng A gửi một bình luận mới. Xác nhận xem Người dùng B có tự động thấy bình luận đó xuất hiện trong thời gian thực mà không cần reload trang hay đóng/mở lại modal không.
  3. Người dùng A xóa bình luận của mình. Xác nhận xem bình luận đó có tự động biến mất trên màn hình của Người dùng B trong thời gian thực không.
* **Kết quả**:
  * Khi Người dùng A gửi bình luận, backend phát ra sự kiện `comment:added`. Frontend của Người dùng B nhận được và làm mới cache `['comments', taskId]`, giúp bình luận mới xuất hiện ngay lập tức.
  * Khi Người dùng A xóa bình luận, backend phát ra sự kiện `comment:deleted`. Frontend của Người dùng B nhận được và làm mới cache `['comments', taskId]`, giúp bình luận biến mất khỏi danh sách ngay lập tức.
* **Trạng thái**: PASS

### [x] Kiểm tra bảo vệ dữ liệu khi chạy Seed & Deploy
* **Kịch bản**:
  1. Sửa đổi `seed.ts` để sử dụng `upsert` riêng biệt cho từng tài khoản mặc định (nhập mật khẩu đã băm `password123`) và chỉ tạo tasks mẫu khi dự án chưa có nhiệm vụ nào.
  2. Dọn dẹp các biến chưa sử dụng (`admin`, `task3`, `task4`) để tránh lỗi biên dịch TypeScript nghiêm ngặt (`noUnusedLocals`).
  3. Thực thi chạy seed dữ liệu cục bộ bằng `npx prisma db seed`.
  4. Kiểm tra danh sách người dùng trong cơ sở dữ liệu để đảm bảo các tài khoản cũ không bị xóa/ghi đè, đồng thời các tài khoản mặc định mới đã tồn tại.
* **Kết quả**:
  * Biên dịch TypeScript của seed script thành công 100%.
  * Quá trình seeding hoàn tất thành công, khởi tạo đầy đủ các tài khoản mặc định (`admin@taskflow.com`, `leader@taskflow.com`, `member@taskflow.com`) và giữ nguyên tài khoản cũ (`test@example.com` vẫn giữ nguyên tên mới thay đổi).
  * `npx prisma db push` cập nhật schema an toàn mà không làm mất dữ liệu.
* **Trạng thái**: PASS

---

## II. KIỂM THỬ TỰ ĐỘNG (AUTOMATED TEST SUITES)

### 1. Backend Integration Tests (Vitest + Supertest)
* **Kết quả**:
  * `auth.test.ts`: 5/5 tests PASS
  * `project.test.ts`: 5/5 tests PASS
  * `task.test.ts`: 6/6 tests PASS (bao gồm ca kiểm thử chặn tự phản hồi bình luận)
* **Trạng thái**: **PASS 100% (16/16 tests)**

### 2. Frontend Unit & Component Tests (Vitest + React Testing Library)
* **Kết quả**:
  * `PriorityTag.test.tsx`: 4/4 tests PASS
  * `MarkdownRenderer.test.tsx`: 5/5 tests PASS
  * `TaskCalendar.test.tsx`: 3/3 tests PASS
* **Trạng thái**: **PASS 100% (12/12 tests)**

---

## III. KIỂM TRA BIÊN DỊCH (COMPILE CHECKS)
* **Frontend (`frontend/`)**: Chạy `npx tsc -b` -> Biên dịch thành công 100%, không lỗi TypeScript.
* **Backend (`backend/`)**: Chạy `npx tsc` -> Biên dịch thành công 100%, không lỗi TypeScript.
