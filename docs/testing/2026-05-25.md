# Nhật ký Kiểm thử (Testing Log) – 2026-05-25

Bản ghi kết quả kiểm thử chức năng ngày hôm nay.

### [x] Khắc phục lỗi thứ tự gọi Hooks (Rules of Hooks violation)
- **Kịch bản**: Vào trang chi tiết dự án sau khi sửa Hooks.
- **Trạng thái**: PASS (Chức năng tải dữ liệu dự án hoạt động bình thường, không crash runtime).

### [x] Quản lý Công việc con (Subtasks / Checklist)
- **Kịch bản**: CRUD subtasks và kiểm tra phân quyền cho Member không được gán.
- **Trạng thái**: PASS (Thao tác dữ liệu thành công, phân quyền block đúng mã 403 ở backend khi truy cập trái phép).

### [x] Hệ thống Nhãn Tùy chỉnh (Custom Project Labels)
- **Kịch bản**: CRUD nhãn dự án và gán nhãn cho task.
- **Trạng thái**: PASS (Tạo, gán và xóa nhãn hoạt động chính xác ở cả frontend API và backend).

### [x] Xuất báo cáo dữ liệu dự án ra CSV (Project Data Export)
- **Kịch bản**: Gọi API xuất dữ liệu dự án ra CSV và tải file về máy.
- **Trạng thái**: PASS (Tải file thành công, dữ liệu CSV đúng cấu trúc và hiển thị đúng tiếng Việt).

### [x] Kiểm thử tích hợp tự động (Automated Integration Tests)
- **Kịch bản**: Chạy script API test tự động giả lập Leader và Member.
- **Trạng thái**: PASS (Tất cả 13 kịch bản tích hợp API thành công 100%).

### [x] Khắc phục lỗi crash khi nhận thông báo Real-time
- **Kịch bản**: Người dùng A và Người dùng B cùng đăng nhập. Người dùng B bình luận trên một task được gán cho Người dùng A. Kiểm tra xem Người dùng A có nhận được thông báo toast mà không bị crash giao diện hay không.
- **Trạng thái**: PASS (Nhận thông báo bình luận thành công, hiển thị toast mượt mà và không còn lỗi React child object crash).

### [x] Hệ thống Phân định Trách nhiệm RACI (RACI Matrix)
- **Kịch bản**: CRUD nhiệm vụ chứa các trường RACI (Accountable, Consulted, Informed), truy vấn và phân giải thông tin User liên quan cho mỗi vai trò, kiểm tra cập nhật và ghi nhận lịch sử thay đổi RACI.
- **Trạng thái**: PASS (Kiểm thử cơ sở dữ liệu và tích hợp API thành công 100%).
