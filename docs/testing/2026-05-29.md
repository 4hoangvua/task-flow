# Nhật ký Kiểm thử (Testing Log) – 2026-05-29

Bản ghi kết quả kiểm thử chức năng và kiểm thử tự động sau khi triển khai tính năng **Lịch biểu dự án (Gantt Chart / Timeline View)**.

---

## I. KIỂM THỬ CHỨC NĂNG (MANUAL FUNCTIONAL TEST CASES)

### [x] Kiểm tra hiển thị sơ đồ Gantt
* **Kịch bản**: 
  1. Đăng nhập hệ thống, truy cập trang chi tiết dự án.
  2. Click chuyển sang Tab "Lịch biểu (Timeline)".
  3. Kiểm tra xem danh sách công việc hiển thị đúng ở cột trái và sơ đồ thanh ngang Gantt hiển thị đúng ở trục thời gian bên phải hay không.
* **Kết quả**: 
  * Cột bên trái hiển thị danh sách tên công việc, người thực hiện, thời gian bắt đầu, hạn chót và độ ưu tiên.
  * Khung bên phải hiển thị chính xác các thanh Gantt màu tương ứng với trạng thái (grey cho TODO, indigo cho IN_PROGRESS, violet cho REVIEW, emerald cho DONE).
  * Lưới thời gian ngày/tuần hiển thị đầy đủ, đồng bộ cuộn ngang giữa tiêu đề và nội dung.
* **Trạng thái**: PASS

### [x] Kiểm tra liên kết mối quan hệ phụ thuộc (Task Dependencies) trên sơ đồ Gantt
* **Kịch bản**:
  1. Gán liên kết phụ thuộc giữa Task A và Task B (Task B phụ thuộc Task A).
  2. Xem Tab Lịch biểu, kiểm tra xem có đường nối mũi tên SVG xuất hiện từ cuối thanh Task A đến đầu thanh Task B hay không.
  3. Đổi trạng thái Task A sang DONE, kiểm tra xem đường nối có chuyển màu (từ màu xanh nét đứt sang xanh lá nét liền) tương thích trạng thái hay không.
* **Kết quả**:
  * Các đường mũi tên SVG vẽ chính xác các mối quan hệ phụ thuộc.
  * Khi công việc tiên quyết hoàn thành, đường nối tự động đổi sang màu xanh lá (`#34d399`) và dạng nét liền biểu thị mối liên kết đã thỏa mãn.
* **Trạng thái**: PASS

### [x] Kiểm tra tạo và cập nhật Ngày bắt đầu (Start Date) của công việc
* **Kịch bản**:
  1. Leader click "Công việc mới" trên bảng Kanban hoặc Lịch biểu.
  2. Chọn Ngày bắt đầu và Hạn chót. Kiểm tra ràng buộc validation trên form (chặn chọn ngày bắt đầu sau hạn chót).
  3. Bấm "Tạo mới". Mở modal chi tiết công việc đó và kiểm tra hiển thị Ngày bắt đầu.
  4. Bấm "Chỉnh sửa", đổi ngày bắt đầu và lưu lại, kiểm tra lịch sử hoạt động ghi nhận thay đổi `startDate`.
* **Kết quả**:
  * Form chặn gửi và báo lỗi nếu ngày bắt đầu lớn hơn hạn chót.
  * Công việc được tạo thành công lưu đúng `startDate` vào DB.
  * Chi tiết công việc hiển thị Ngày bắt đầu song song với Hạn chót.
  * Lịch sử hoạt động ghi nhận đúng hành động thay đổi ngày bắt đầu.
* **Trạng thái**: PASS

### [x] Kiểm tra bộ lọc trên sơ đồ Gantt
* **Kịch bản**:
  1. Nhập từ khóa tìm kiếm trong ô "Tìm kiếm công việc...".
  2. Chọn lọc theo Người thực hiện, Trạng thái, hoặc Độ ưu tiên.
  3. Xác nhận danh sách hiển thị và các thanh Gantt lọc đúng tương ứng.
* **Kết quả**:
  * Lọc hoạt động tức thì, ẩn/hiện đúng các công việc khớp bộ lọc.
  * Các đường nối phụ thuộc chỉ vẽ giữa các công việc đang được hiển thị, tránh rối mắt.
* **Trạng thái**: PASS

### [x] Kiểm tra hiển thị Trang Hướng dẫn sử dụng (Help Page)
* **Kịch bản**:
  1. Đăng nhập hệ thống, click mục "Hướng dẫn sử dụng" trên thanh Sidebar điều hướng.
  2. Kiểm tra xem trang có load đúng danh mục mặc định "Giới thiệu" không.
  3. Kiểm tra xem giao diện màu Obsidian tối và cách trình bày bento grid có nhất quán không.
* **Kết quả**:
  * Trang Hướng dẫn sử dụng hiển thị mượt mà.
  * Tông màu tối Obsidian và Zinc đồng bộ hoàn hảo với hệ thống. Các biểu tượng hiển thị trực quan và sắc nét.
* **Trạng thái**: PASS

### [x] Kiểm tra chuyển đổi danh mục tài liệu & Privilege Matrix
* **Kịch bản**:
  1. Lần lượt click vào các danh mục bên trái (Bắt đầu nhanh, Vai trò & Phân quyền, Bảng Kanban...).
  2. Tại danh mục "Vai trò & Phân quyền", kiểm tra xem bảng so sánh quyền giữa Admin, Leader và Member có hiển thị đúng với các biểu tượng check/close màu sắc không.
* **Kết quả**:
  * Việc chuyển đổi danh mục diễn ra lập tức mà không có độ trễ.
  * Bảng Privilege Matrix hiển thị chi tiết, rõ ràng và bắt mắt.
* **Trạng thái**: PASS

### [x] Kiểm tra bộ lọc tìm kiếm động tài liệu
* **Kịch bản**:
  1. Nhập từ khóa tìm kiếm (ví dụ: "mật khẩu", "phụ thuộc", "leader") vào thanh tìm kiếm ở đầu trang.
  2. Xác nhận hệ thống tự động lọc toàn bộ tài liệu và hiển thị các mục tương thích.
  3. Bấm nút xóa (Clear) trong ô tìm kiếm để quay lại chế độ duyệt danh mục thông thường.
* **Kết quả**:
  * Bộ lọc tìm kiếm hoạt động tức thì, hiển thị chính xác các kết quả tìm thấy tương thích với từ khóa và nhãn từ khóa.
  * Thao tác Clear khôi phục lại trạng thái xem danh mục bình thường.
* **Trạng thái**: PASS

---

## II. KIỂM THỬ TỰ ĐỘNG (AUTOMATED TEST SUITES)

### 1. Backend Integration Tests (Vitest + Supertest)
* **Kết quả**:
  * `task.test.ts`: Đã thêm test case `should support startDate creation, updates and validate startDate <= deadline`.
  * Chạy `npm.cmd test` trong thư mục `backend/` thành công 100% (17/17 tests PASS).
* **Trạng thái**: **PASS 100%**

### 2. Frontend Unit & Component Tests (Vitest + React Testing Library)
* **Kết quả**:
  * Đã tạo mới file test `TaskTimeline.test.tsx` kiểm thử 3 kịch bản:
    1. Render timeline header, inputs, và task rows.
    2. Click dòng công việc mở modal chi tiết công việc.
    3. Click nút "Công việc mới" mở modal tạo công việc.
  * Chạy `npm.cmd test` trong thư mục `frontend/` thành công 100% (15/15 tests PASS, 0 warnings).
* **Trạng thái**: **PASS 100%**

---

## III. KIỂM TRA BIÊN DỊCH (COMPILE CHECKS)
* **Frontend (`frontend/`)**: Chạy `npx.cmd tsc -b` -> Biên dịch thành công 100%, không lỗi TypeScript.
* **Backend (`backend/`)**: Chạy `npx.cmd tsc` -> Biên dịch thành công 100%, không lỗi TypeScript.
