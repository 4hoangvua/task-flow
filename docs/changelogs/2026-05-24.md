# Changelog – 2026-05-24

### [x] Sửa lỗi cảnh báo Ant Design & Chữ mờ chế độ Sáng/Tối
- [x] Khắc phục cảnh báo Ant Design bằng cách thay thế thuộc tính cũ `bodyStyle` thành `styles={{ body: ... }}` trên component `<Card>` trong [Projects.tsx](file:///c:/project/task-flow/frontend/src/pages/Projects.tsx) và [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx).
- [x] Khai báo các biến CSS `--text-secondary` và `--text-tertiary` trong [index.css](file:///c:/project/task-flow/frontend/src/index.css) để giải quyết triệt để lỗi không rõ chữ ở cả chế độ sáng và chế độ tối.
- [x] Tạo global static helper [antd.ts](file:///c:/project/task-flow/frontend/src/utils/antd.ts) kế thừa context của `<App>` và điều chỉnh tất cả file sử dụng `message` tĩnh để loại bỏ cảnh báo `Static function can not consume context`.
- [x] Trì hoãn việc gọi `settingsForm.setFieldsValue` bằng `setTimeout` trong [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) để triệt tiêu hoàn toàn cảnh báo `useForm is not connected to any Form element`.


### [x] Thiết kế giao diện Responsive Mobile-First
- [x] **Sidebar Navigation**: Cập nhật logic thu gọn Sider trên Mobile và tích hợp component `<Drawer>` trong [DashboardLayout.tsx](file:///c:/project/task-flow/frontend/src/layouts/DashboardLayout.tsx) để mở rộng menu điều hướng bằng slide-over trên màn hình nhỏ.
- [x] **Trang Dự án**: Khóa chế độ xem Grid Card và ẩn các nút chuyển đổi Grid/List trên thiết bị di động trong [Projects.tsx](file:///c:/project/task-flow/frontend/src/pages/Projects.tsx).
- [x] **Kanban Board**: Tích hợp thanh `<Segmented>` trong [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx) để chỉ hiển thị một cột công việc tại một thời điểm trên mobile; loại bỏ hoàn toàn các thuộc tính set cứng độ rộng cột (`min-w-[280px]` và `shrink-0`) trên môi trường desktop/tablet bằng cách sử dụng `min-w-0` giúp các cột tự động co giãn ôm khít màn hình mà không bị tràn hay gây scroll x.
- [x] **Công việc của tôi**: Thay thế Table danh sách công việc bằng bố cục dọc các Task Card nhỏ gọn trên màn hình nhỏ trong [MyTasks.tsx](file:///c:/project/task-flow/frontend/src/pages/MyTasks.tsx).

- [x] **Thành viên dự án**: Responsive hóa danh sách thành viên trong [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) bằng cách ẩn Table và hiển thị hàng Member Card gọn gàng xếp chồng dọc trên mobile.

### [x] Triển khai Giao diện & Chức năng Quản trị Hệ thống (System Admin Workspace)
- [x] **Backend - API Quản trị**:
  - Triển khai [adminController.ts](file:///c:/project/task-flow/backend/src/controllers/adminController.ts) và [adminRoutes.ts](file:///c:/project/task-flow/backend/src/routes/adminRoutes.ts) cung cấp 4 endpoint: Lấy danh sách users kèm bộ lọc/tìm kiếm, thay đổi vai trò hệ thống (`role`), khóa/mở tài khoản (`isActive`) và thống kê hệ thống (Bento Stats).
  - Tích hợp bảo vệ bằng `authMiddleware` và `requireSystemRole(['ADMIN'])`. Mount route tại `/api/admin` trong [routes/index.ts](file:///c:/project/task-flow/backend/src/routes/index.ts).
  - Sửa lỗi cảnh báo compiler TS6133 của TS bằng cách đổi biến chưa dùng `req` thành `_req` trong `getSystemStats`.
- [x] **Frontend - Quản lý Admin**:
  - Tạo tệp call API [adminApi.ts](file:///c:/project/task-flow/frontend/src/api/adminApi.ts) và custom hooks React Query [useAdmin.ts](file:///c:/project/task-flow/frontend/src/hooks/useAdmin.ts) để quản lý state và mutation.
  - Xây dựng trang [AdminDashboard.tsx](file:///c:/project/task-flow/frontend/src/pages/AdminDashboard.tsx) đồng bộ hoàn toàn theo bản thiết kế **Obsidian Executive Dashboard** từ Stitch (KPI Bento Grid, bảng danh sách người dùng với Switch toggle và Select role).
  - Cấu hình Sidebar trong [DashboardLayout.tsx](file:///c:/project/task-flow/frontend/src/layouts/DashboardLayout.tsx) để hiển thị thêm menu "Quản trị hệ thống" duy nhất cho tài khoản `ADMIN`.
  - Khai báo route bảo vệ `/admin` bằng `RoleGuard` trong [App.tsx](file:///c:/project/task-flow/frontend/src/App.tsx) để ngăn chặn MEMBER/LEADER truy cập trái phép.

### [x] Tính năng Tìm kiếm Email & Tự động gợi ý thành viên dự án
- [x] Cập nhật hàm tìm kiếm người dùng trong [authController.ts](file:///c:/project/task-flow/backend/src/controllers/authController.ts) để trả về 10 tài khoản hoạt động đầu tiên khi chuỗi truy vấn rỗng, hỗ trợ hiển thị danh sách email gợi ý sẵn.
- [x] Tích hợp thuộc tính `onFocus`, `loading` và thiết lập `filterOption={false}` cho `<AutoComplete>` trong [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) giúp tải, hiển thị danh sách người dùng và tắt bộ lọc client của Ant Design để hiển thị các gợi ý từ server chính xác nhất.

### [x] Cấu hình Server AWS (AWS Production Server Configuration)
- [x] SSH vào server AWS (`54.173.190.122`) và cập nhật cấu hình Nginx trong `/etc/nginx/sites-available/task-flow` từ địa chỉ IP cũ `13.223.226.199` thành IP mới `54.173.190.122`.
- [x] Kiểm tra và tải lại cấu hình Nginx thành công (`sudo nginx -t && sudo systemctl reload nginx`).
- [x] Cập nhật biến môi trường `CLIENT_URL` in `/home/task-flow-backend/backend/.env` trên server trỏ về IP mới `54.173.190.122`.
- [x] Khởi chạy backend bằng PM2 trên server (`pm2 start dist/index.js --name 'task-flow-backend'`) và kiểm tra tính ổn định.
- [x] Cập nhật tệp cấu hình [frontend/.env](file:///C:/project/task-flow/frontend/.env) tại local máy người dùng để kết nối chính xác tới địa chỉ backend AWS mới.
- [x] Khắc phục lỗi CORS/Private Network Access bằng cách tạo cấu hình production `/var/www/task-flow/frontend/.env.production` chứa `VITE_API_URL=/api` và `VITE_SOCKET_URL=/` trên server AWS, sau đó rebuild frontend bằng `npm run build` để tận dụng Nginx proxy và tránh gọi nhầm sang `localhost:5000` từ trình duyệt của client.

### [x] Chuyển đổi công cụ Tìm kiếm sang AutoComplete gợi ý (Search AutoComplete Component)
- [x] Tạo component dùng chung [SearchAutoComplete.tsx](file:///c:/project/task-flow/frontend/src/components/common/SearchAutoComplete.tsx) kế thừa `<AutoComplete>` của Ant Design, tự động lọc và gợi ý kết quả khớp theo thời gian thực (được tối ưu hóa tối đa 8 gợi ý hiển thị).
- [x] Áp dụng component mới vào trang [Projects.tsx](file:///c:/project/task-flow/frontend/src/pages/Projects.tsx) để gợi ý tên dự án kèm đoạn trích dẫn mô tả ngắn.
- [x] Áp dụng component mới vào trang [MyTasks.tsx](file:///c:/project/task-flow/frontend/src/pages/MyTasks.tsx) để gợi ý công việc cá nhân kèm tag trạng thái chi tiết.
- [x] Áp dụng component mới vào [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx) phục vụ tìm kiếm nhiệm vụ trên bảng Kanban của dự án.
- [x] Khắc phục triệt để lỗi biên dịch TypeScript: loại bỏ thuộc tính không hợp lệ `loading` khỏi AutoComplete trong [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) và sửa kiểu dữ liệu `modal` tĩnh thành `any` để tương thích hoàn hảo giữa `HookAPI` và `ModalStaticFunctions` trong [antd.ts](file:///c:/project/task-flow/frontend/src/utils/antd.ts).

### [x] Dọn dẹp Logs & Cấu hình Bỏ theo dõi Git cho các tệp .env
- [x] Tắt và loại bỏ các dòng `console.log` và `console.error` debug khỏi hàm `handleSearchUser` trong [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) để giữ log sạch sẽ.
- [x] Loại bỏ (untrack) các tệp `frontend/.env` và `frontend/.env.production` khỏi Git cache bằng `git rm --cached` và thêm cấu hình loại trừ vào [.gitignore](file:///c:/project/task-flow/.gitignore) để đảm bảo không bị push thông tin nhạy cảm lên GitHub.
- [x] Xóa hoàn toàn các tệp hình ảnh chụp màn hình UI trong thư mục `frontend/` (`dashboard_screen.png`, `kanban_screen.png`, `login_screen.png`, `projects_screen.png`, `settings_screen.png`) theo yêu cầu.
- [x] Tối giản hóa quy trình deploy trong [deploy.yml](file:///c:/project/task-flow/.github/workflows/deploy.yml) bằng cách loại bỏ các commented jobs dư thừa và phần hướng dẫn dài, giữ lại luồng Deploy SSH Pull tinh gọn và an toàn cho database.

### [x] Triển khai dịch vụ chạy ngầm tự động thông báo Hạn chót công việc (DEADLINE_APPROACHING)
- [x] Tạo file tiện ích chạy ngầm [scheduler.ts](file:///c:/project/task-flow/backend/src/utils/scheduler.ts) truy vấn định kỳ mỗi giờ để phát hiện các task chưa DONE, có assignee, và có hạn chót trong vòng 24 giờ tới.
- [x] Tạo thông báo `DEADLINE_APPROACHING` và lưu vào cơ sở dữ liệu cho các task thỏa mãn điều kiện và chưa có thông báo tương ứng.
- [x] Phát sự kiện Socket.io thời gian thực tới room của người dùng (`user:assigneeId`) để cập nhật chuông thông báo lập tức ở giao diện client.
- [x] Khởi tạo và liên kết dịch vụ chạy ngầm ngay trên server startup trong file chính [index.ts](file:///c:/project/task-flow/backend/src/index.ts).





