# Changelog – 2026-05-26

### [x] Cấu hình tên miền & CORS (Domain & CORS Configuration)
- [x] SSH cập nhật cấu hình Nginx `/etc/nginx/sites-available/task-flow` trên server AWS trỏ về tên miền mới `hoangtran.name.vn`.
- [x] Cập nhật tệp cấu hình `.env` của backend trên server để thêm tên miền `hoangtran.name.vn` (cả biến thể HTTP, HTTPS và WWW) vào danh sách CORS allowed origins.
- [x] Khắc phục xung đột tiến trình PM2 trên server: Xóa tiến trình trùng lặp cũ tên là `index` và khởi động lại tiến trình chính `task-flow-backend` với cờ `--update-env` để nạp thành công CORS config mới lên port 5000.

### [x] Tài liệu hóa thông tin Server (Server Access Documentation)
- [x] Thêm mục `6. Server Access & Directory Mapping` vào tài liệu [PLAN.md](file:///C:/project/task-flow/docs/PLAN.md) chi tiết hóa đường dẫn SSH, IP server, tên miền cấu hình, khóa private key `hoang.pem` và cấu trúc các thư mục Deploy frontend/backend trên server AWS.

### [x] Tính năng Công việc Phụ thuộc (Task Dependencies)
- [x] Cập nhật database [schema.prisma](file:///c:/project/task-flow/backend/prisma/schema.prisma) để thêm model `TaskDependency` thể hiện quan hệ tự liên kết trên `Task`, chạy thành công `prisma generate` và `prisma db push`.
- [x] Triển khai Zod validation schema trong [validation.ts](file:///c:/project/task-flow/backend/src/utils/validation.ts) và controllers trong [taskController.ts](file:///c:/project/task-flow/backend/src/controllers/taskController.ts):
  - Viết API `addDependency` (`POST /api/tasks/:id/dependencies`) hỗ trợ ràng buộc tự phụ thuộc và phát hiện chu trình tuần hoàn trực tiếp/gián tiếp qua thuật toán đệ quy DFS `hasCircularPath`.
  - Viết API `removeDependency` (`DELETE /api/tasks/:id/dependencies/:dependsOnId`).
  - Cản trở chuyển trạng thái task sang `DONE` trong `updateTaskStatus` và `reorderTasks` khi có công việc tiên quyết chưa hoàn thành, trả về mã lỗi 400.
- [x] Đăng ký các endpoints mới trong [taskRoutes.ts](file:///c:/project/task-flow/backend/src/routes/taskRoutes.ts) được bảo vệ bằng RBAC.
- [x] Định nghĩa types `TaskDependency` mới trong [types/index.ts](file:///c:/project/task-flow/frontend/src/types/index.ts).
- [x] Tích hợp frontend calls và mutations trong [taskApi.ts](file:///c:/project/task-flow/frontend/src/api/taskApi.ts) và [useTasks.ts](file:///c:/project/task-flow/frontend/src/hooks/useTasks.ts).
- [x] Thiết kế UI quản lý phụ thuộc trong [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) hiển thị các prerequisites có cảnh báo và dependents, tích hợp Select dropdown tìm kiếm/thêm nhanh task tiên quyết hợp lệ trong dự án (lọc bỏ các task tạo vòng lặp gián tiếp qua thuật toán DFS client-side `isReachable`).
- [x] Hiển thị biểu tượng liên kết `🔗 {count}` trên Kanban card trong [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx) khi công việc có dependencies.
- [x] Biên dịch TypeScript cả Frontend (`npx tsc -b`) và Backend (`npx tsc`) thành công 100% không lỗi kiểu dữ liệu.
- [x] Ghi nhận nhật ký kiểm thử functional test trong [2026-05-26.md](file:///c:/project/task-flow/docs/testing/2026-05-26.md) đạt trạng thái PASS 100% cho 5 kịch bản chính.

### [x] Khắc phục Bảo mật Team Charter & Quyền Xóa Bình luận
- [x] Áp dụng middleware `requireProjectRole` cho các route của Team Charter trong [charterRoutes.ts](file:///c:/project/task-flow/backend/src/routes/charterRoutes.ts) để ngăn chặn IDOR/BOLA.
- [x] Lược bỏ logic xác thực quyền thủ công trong `updateCharter` tại [charterController.ts](file:///c:/project/task-flow/backend/src/controllers/charterController.ts).
- [x] Nâng cấp quyền xóa bình luận trong `deleteComment` tại [commentController.ts](file:///c:/project/task-flow/backend/src/controllers/commentController.ts) để cho phép Project LEADER xóa bình luận của các thành viên khác trong dự án.

### [x] Thành viên tự sở hữu dự án & Phân chia Danh sách Dự án (Personal Projects & Tab Separation)
- [x] Cập nhật định tuyến dự án trên Backend trong [projectRoutes.ts](file:///c:/project/task-flow/backend/src/routes/projectRoutes.ts) cho phép người dùng hệ thống có vai trò `MEMBER` được phép gọi `POST /api/projects`.
- [x] Cập nhật giao diện Frontend trong [Projects.tsx](file:///c:/project/task-flow/frontend/src/pages/Projects.tsx) để hiển thị nút "Tạo dự án mới" cho tất cả người dùng.
- [x] Tái cấu trúc giao diện trang Tổng quan trong [Dashboard.tsx](file:///c:/project/task-flow/frontend/src/pages/Dashboard.tsx) sang dạng **Tabs** (Cá nhân & Dự án) giúp hiển thị biểu đồ và số liệu thống kê dự án cho cả `MEMBER` khi họ sở hữu/tham gia dự án.
- [x] Phân chia danh sách dự án trong [Projects.tsx](file:///c:/project/task-flow/frontend/src/pages/Projects.tsx) bằng cách thêm Tabs ("Tất cả", "Dự án sở hữu", "Dự án tham gia") kèm số lượng đếm động của dự án.
- [x] Biên dịch TypeScript cả Frontend (`npx tsc -b`) và Backend (`npx tsc`) thành công 100% không lỗi kiểu dữ liệu.
- [x] Thêm nhật ký kiểm thử functional test trong [2026-05-26.md](file:///c:/project/task-flow/docs/testing/2026-05-26.md) đạt trạng thái PASS 100%.
### [x] Hiển thị Người thực hiện trong Chi tiết Công việc (Assignee Display in Task Details)
- [x] Bổ sung trường "NGƯỜI THỰC HIỆN" (Assignee) vào panel thông tin bên phải trong [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) hiển thị Avatar và tên người được giao việc (hoặc hiển thị "Chưa gán" nếu null).

### [x] Khắc phục Sai lệch Thứ tự Công việc (Task Reordering Gaps & Conflicts Fixes)
- [x] Cập nhật hàm `deleteTask` tại [taskController.ts](file:///c:/project/task-flow/backend/src/controllers/taskController.ts) sử dụng transaction giảm `order` của các công việc đứng sau trong cùng cột để tránh để lại khoảng trống (gap) khi một task bị xóa.
- [x] Cập nhật hàm `updateTaskStatus` tại [taskController.ts](file:///c:/project/task-flow/backend/src/controllers/taskController.ts) sử dụng transaction giảm `order` cột cũ, tự động gán `order` cuối cột mới cho task và bổ sung kiểm tra Project Owner để đồng bộ phân quyền.
