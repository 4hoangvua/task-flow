# Changelog – 2026-05-25

### [x] Cập nhật Quy quy trình Phát triển Tự động & Hướng dẫn Nghiên cứu của Agent
- [x] Làm rõ lựa chọn phạm vi nghiên cứu là **Frontend**, **Backend**, hoặc **Cả hai** đối với tính năng/chức năng mới trong quy trình tự phát triển tại [auto-develop.md](file:///c:/project/task-flow/.agents/workflows/auto-develop.md) và hướng dẫn nghiên cứu [analysis.md](file:///c:/project/task-flow/.agents/rules/analysis.md).
- [x] Cập nhật quy tắc bắt buộc Agent phải chủ động tìm kiếm, đề xuất nghiên cứu các tính năng/chức năng mới thiết thực (chứ không chỉ dừng lại ở sửa đổi nhỏ hay tối ưu hóa/refactor mã nguồn).
- [x] Bổ sung quy tắc **Design-First với Stitch**: Bắt buộc sử dụng các công cụ Stitch MCP (như `generate_screen_from_text`, `edit_screens`, v.v.) để phác thảo và vẽ trước giao diện trực quan trước khi tiến hành code frontend đối với bất kỳ giao diện mới hoặc nâng cấp/cải tiến giao diện nào.

### [x] Tối ưu hóa tương thích Mobile & Tự động Rà soát
- [x] Cập nhật Drawer điều hướng trên mobile trong [DashboardLayout.tsx](file:///c:/project/task-flow/frontend/src/layouts/DashboardLayout.tsx) sử dụng đúng thuộc tính `width={280}` thay vì `size={280}` để sửa cảnh báo Ant Design và đảm bảo kích thước chuẩn trên thiết bị di động.
- [x] Tối ưu hóa bố cục tiêu đề của [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) tự động chuyển sang cột dọc (`flex-col`) trên màn hình nhỏ để tránh dồn ép và tràn nút bấm Chỉnh sửa/Xóa; đồng bộ hóa kích thước nút (`size`) linh hoạt theo trạng thái `isMobile` (sử dụng `'small'` trên di động và `'middle'` trên máy tính).
- [x] Triển khai chế độ xem danh sách Card cho người dùng (User Card List) khi thiết bị truy cập là di động (`isMobile === true`) trong trang [AdminDashboard.tsx](file:///c:/project/task-flow/frontend/src/pages/AdminDashboard.tsx), tự động ẩn Table để tối ưu không gian hiển thị và thao tác quản lý.

### [x] Triển khai 3 Chức năng Mới từ Tài liệu Phân tích
- [x] **Quản lý công việc con (Subtasks / Checklist)**:
  - Tích hợp CRUD APIs và React Query mutations (`createSubtask`, `updateSubtask`, `deleteSubtask`) trong [useTasks.ts](file:///c:/project/task-flow/frontend/src/hooks/useTasks.ts).
  - Thiết kế và hoàn thiện UI Checklist trực quan trong [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) hiển thị danh sách checkbox, thanh tiến độ phần trăm hoàn thành, khả năng chỉnh sửa trực tiếp, hover hiện nút xóa và input thêm nhanh subtask.
- [x] **Hệ thống nhãn tùy chỉnh (Custom Project Labels)**:
  - Cập nhật [TaskFormModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskFormModal.tsx) tải danh sách nhãn dự án bằng `useQuery` và cho phép chọn nhiều nhãn thông qua `<Select mode="multiple">`.
  - Hiển thị danh sách nhãn màu sắc nhỏ gọn dưới dạng tag trên Kanban Card ở [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx) và nhãn chi tiết trong [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx).
  - Triển khai **Trình quản lý Nhãn (Label Manager)** trực quan trong tab Cấu hình (Settings) tại [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) hỗ trợ hiển thị, xóa và tạo nhãn mới với palette màu preset.
- [x] **Xuất báo cáo dữ liệu dự án ra CSV (Project Data Export)**:
  - Thêm nút "Xuất báo cáo CSV" trên Header của trang [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) (chỉ hiển thị cho Leader).
  - Tích hợp gọi API tải file CSV dạng Blob bảo mật bằng axios, lưu file download về máy người dùng tương thích hiển thị tiếng Việt trên Microsoft Excel qua UTF-8 BOM.
- [x] **Khắc phục lỗi thứ tự gọi Hooks (Rules of Hooks violation)**:
  - Di chuyển toàn bộ các declarations hooks/states mới (`labels`, `isExporting`, `labelName`, `labelColor`, `isCreatingLabel`) lên đầu component [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) để tránh việc gọi hooks sau các điều kiện return sớm (`!id`, `isLoadingProject`, `!project`).
- [x] **Sửa lỗi trích xuất tham số ID khi tạo Subtask**:
  - Khắc phục lỗi `id: undefined` trong truy vấn Prisma bằng cách chuyển đổi từ `const { taskId } = req.params;` sang `const { id: taskId } = req.params;` trong hàm `createSubtask` tại [subtaskController.ts](file:///c:/project/task-flow/backend/src/controllers/subtaskController.ts) để tương thích với cấu trúc của `requireTaskProjectRole` middleware.
- [x] **Thiết lập Quy trình & Nhật ký Kiểm thử chức năng (Functional Testing)**:
  - Cập nhật tài liệu hướng dẫn phát triển của Agent [AGENTS.md](file:///c:/project/task-flow/AGENTS.md) và workflow phát triển tự động [auto-develop.md](file:///c:/project/task-flow/.agents/workflows/auto-develop.md) bổ sung bước kiểm thử chức năng bắt buộc, đồng thời làm rõ ranh giới: chỉ kiểm thử chức năng, logic nghiệp vụ, API, không dùng mã gọi API để test giao diện trực quan (UI/Layout).
  - Tạo tệp nhật ký kiểm thử đầu tiên [2026-05-25.md](file:///c:/project/task-flow/docs/testing/2026-05-25.md) ghi nhận 5 kịch bản kiểm thử (Hooks order, Subtasks, Custom Labels, CSV Export, Integration Test) đều đạt trạng thái **PASS 100%**.
- [x] Kiểm thử biên dịch TypeScript (tsc):
  - Kiểm tra biên dịch ở cả frontend (`npx tsc -b`) và backend (`npx tsc`) chạy thành công 100% với 0 lỗi kiểu dữ liệu.

### [x] Khắc phục giao diện di động (Mobile UI) & Cuộn trang
- [x] Thay thế Segmented tabs bị chèn ép, quá hẹp trên mobile thành thanh nút cuộn ngang (horizontal scrollbar-none tabs) linh hoạt, hiển thị rõ nét kèm theo số lượng task trong mỗi tab tại [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx).
- [x] Loại bỏ thanh cuộn dọc lồng nhau (nested scrollbar) của DroppableColumn trên thiết bị di động bằng cách đổi chiều cao sang tự động (`h-auto` & tắt `overflow-y-auto`) khi ở mobile, giúp cuộn trang tự nhiên và tránh bị kẹt thao tác cuộn tại [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx).
- [x] Tăng padding bottom (`pb-24`) và giảm padding ngang (`px-4`) của vùng nội dung chính Content trên màn hình nhỏ tại [DashboardLayout.tsx](file:///c:/project/task-flow/frontend/src/layouts/DashboardLayout.tsx), đảm bảo các nút ở cuối trang (như Danger Zone, Tạo nhãn mới) không bị che lấp bởi các thanh công cụ/navigation bar của trình duyệt di động và có thể kéo xuống hoàn toàn.
- [x] Khắc phục lệch hiển thị và tràn viền của dropdown danh sách thông báo trên di động bằng cách chuyển đổi sang dùng ngăn kéo di động (Mobile Drawer) trượt từ cạnh phải khi nhấp chuông thông báo trên màn hình nhỏ tại [DashboardLayout.tsx](file:///c:/project/task-flow/frontend/src/layouts/DashboardLayout.tsx).
- [x] Sửa lỗi che lấp/không hiển thị hết các tab khi kéo/cuộn ngang đến tab cuối cùng trong [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) bằng cách xóa bỏ lề âm `-1rem` và chiều cao/độ rộng calc trên mobile tại [index.css](file:///c:/project/task-flow/frontend/src/index.css), giúp thanh tab nằm gọn và tự động cuộn hoàn toàn bên trong vùng đệm của Content.
- [x] Thêm lớp tiện ích `.scrollbar-none` vào [index.css](file:///c:/project/task-flow/frontend/src/index.css) để ẩn thanh cuộn nhưng giữ nguyên tính năng cuộn ngang mượt mà.

### [x] Việt hóa & Chuẩn hóa Ngôn ngữ Ứng dụng
- [x] Bản địa hóa các thông báo hệ thống (Notifications) từ tiếng Anh sang tiếng Việt tại các API của Backend:
  - Việt hóa tiêu đề và nội dung thông báo giao việc: "Task Assigned" -> "Nhiệm vụ được giao", "Task Reassigned" -> "Giao lại nhiệm vụ" tại [taskController.ts](file:///c:/project/task-flow/backend/src/controllers/taskController.ts).
  - Việt hóa thông báo thay đổi trạng thái công việc: "Task Status Updated" -> "Cập nhật trạng thái công việc" và chuyển đổi trạng thái hiển thị (TODO/IN_PROGRESS/REVIEW/DONE) và vai trò người đổi sang dạng tiếng Việt thân thiện tại [taskController.ts](file:///c:/project/task-flow/backend/src/controllers/taskController.ts).
  - Việt hóa thông báo mời thành viên vào dự án: "Project Invitation" -> "Lời mời vào dự án" và lấy tự động tên dự án thay thế cho ID thô tại [projectController.ts](file:///c:/project/task-flow/backend/src/controllers/projectController.ts).
  - Việt hóa thông báo có bình luận mới: "New Comment Added" -> "Bình luận mới" tại [commentController.ts](file:///c:/project/task-flow/backend/src/controllers/commentController.ts).
  - Việt hóa thông báo mẫu khởi tạo trong cơ sở dữ liệu tại [seed.ts](file:///c:/project/task-flow/backend/prisma/seed.ts).
- [x] Chuẩn hóa ngôn ngữ hiển thị vai trò (MEMBER/LEADER/ADMIN) của người dùng ở Frontend:
  - Bản địa hóa vai trò của thành viên trong danh sách thành viên dự án (màn hình desktop và di động) tại [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx).
  - Việt hóa hiển thị vai trò tài khoản hiện tại trong màn hình cấu hình hồ sơ cá nhân tại [Settings.tsx](file:///c:/project/task-flow/frontend/src/pages/Settings.tsx).
  - Việt hóa nhãn hiển thị trạng thái hệ thống từ "HEALTHY" sang "HOẠT ĐỘNG TỐT" tại [AdminDashboard.tsx](file:///c:/project/task-flow/frontend/src/pages/AdminDashboard.tsx).
