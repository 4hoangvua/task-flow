# Changelog – 2026-05-28

### [x] Khắc phục và Gieo dữ liệu Cơ sở dữ liệu trên Máy chủ (Server Database Seeding & Maintenance)
- [x] Xem log lỗi trên máy chủ AWS và xác nhận lỗi 401 Unauthorized khi đăng nhập là do cơ sở dữ liệu trên máy chủ trống rỗng (chưa được gieo dữ liệu hạt giống).
- [x] Thực hiện chạy lệnh gieo dữ liệu hạt giống `npx prisma db seed` trên máy chủ AWS để thiết lập các tài khoản khởi tạo mặc định (`admin@taskflow.com`, `leader@taskflow.com`, `member@taskflow.com`).
- [x] Khởi động lại tiến trình PM2 `task-flow-backend` trên máy chủ AWS với tham số `--update-env` để áp dụng các thay đổi và kết nối cơ sở dữ liệu sạch.

### [x] Khắc phục Lỗi Bảo mật và Đồng bộ Dữ liệu Hệ thống (Security & Database Integrity Fixes)
- [x] Cập nhật [auth.ts](file:///c:/project/task-flow/backend/src/middlewares/auth.ts) và [socketHandler.ts](file:///c:/project/task-flow/backend/src/sockets/socketHandler.ts) kiểm tra trạng thái hoạt động `isActive: true` của tài khoản, đảm bảo khóa tài khoản bị vô hiệu hóa lập tức khỏi hệ thống API và Socket connections.
- [x] Cập nhật `deleteProjectMember` trong [projectController.ts](file:///c:/project/task-flow/backend/src/controllers/projectController.ts) thực hiện unassign (`assigneeId: null`) toàn bộ công việc trong dự án liên quan đến thành viên bị xóa trong cùng transaction.
- [x] Bổ sung ràng buộc ngày hạn chót ở quá khứ cho `updateTaskSchema` trong [validation.ts](file:///c:/project/task-flow/backend/src/utils/validation.ts).

### [x] Tối ưu hóa UI/UX và Trải nghiệm Người dùng (UI/UX Optimizations)
- [x] Cập nhật hiển thị lỗi nghiệp vụ chi tiết (validation error từ backend) thay vì lỗi Axios chung chung trong `updateTaskStatusMutation` ở [useTasks.ts](file:///c:/project/task-flow/frontend/src/hooks/useTasks.ts).
- [x] Thêm kiểm tra dependencies ngay trên frontend trước khi thay đổi trạng thái sang DONE ở [TaskBoard.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskBoard.tsx), [MyTasks.tsx](file:///c:/project/task-flow/frontend/src/pages/MyTasks.tsx) và [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) nhằm chặn di chuyển DONE và báo lỗi tức thì, tránh nhấp nháy UI.
- [x] Cập nhật [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) để invalidate query `tasks` khi nhận socket event `member:removed`, đảm bảo đồng bộ hóa real-time giao diện người gán công việc.
- [x] Tối ưu hóa gán mặc định dự án và xử lý dự án bị xóa/thu hồi quyền trong `useEffect` ở [Dashboard.tsx](file:///c:/project/task-flow/frontend/src/pages/Dashboard.tsx).
- [x] Điều chỉnh cấu hình mặc định `VITE_API_URL` và `VITE_SOCKET_URL` trong [frontend/.env](file:///c:/project/task-flow/frontend/.env) về `localhost:5000` thay vì IP tĩnh AWS để tránh lỗi preflight CORS redirect khi chạy phát triển local.

### [x] Di chuyển Cơ sở dữ liệu sang Neon PostgreSQL (Migration to Neon PostgreSQL)
- [x] Thay đổi database provider từ `sqlite` sang `postgresql` trong [schema.prisma](file:///c:/project/task-flow/backend/prisma/schema.prisma) để hỗ trợ lưu trữ đám mây Neon.
- [x] Cấu hình chuỗi kết nối `DATABASE_URL` trong [backend/.env](file:///c:/project/task-flow/backend/.env) và [backend/.env.development](file:///c:/project/task-flow/backend/.env.development) hướng tới cơ sở dữ liệu đám mây Neon do người dùng cung cấp.
- [x] Thực hiện đồng bộ hóa schema (`npx prisma db push`) và gieo dữ liệu mẫu ban đầu (`npx prisma db seed`) thành công trên database Neon Cloud (schema `public`).
- [x] Thiết lập cơ chế cô lập cơ sở dữ liệu kiểm thử (Test database isolation) trong [setup.ts](file:///c:/project/task-flow/backend/src/tests/setup.ts): tự động điều chỉnh tham số schema sang `test` trong chuỗi kết nối và thực thi `prisma db push --force-reset` trong không gian schema `test` độc lập, tránh xóa hoặc làm sạch dữ liệu phát triển ở schema `public`.
- [x] Chạy bộ kiểm thử tự động của backend (`npm.cmd test` trong `backend/`) và frontend (`npm.cmd test` trong `frontend/`) xác nhận 15/15 backend tests và 12/12 frontend tests đã hoàn thành và đạt trạng thái PASS 100% trên môi trường kết nối PostgreSQL.

### [x] Khôi phục Cơ sở dữ liệu SQLite local (Reversion to SQLite) & Cô lập Database Kiểm thử
- [x] Khôi phục database provider từ `postgresql` về `sqlite` trong [schema.prisma](file:///c:/project/task-flow/backend/prisma/schema.prisma).
- [x] Khôi phục cấu hình `DATABASE_URL` trong [backend/.env](file:///c:/project/task-flow/backend/.env) và [backend/.env.development](file:///c:/project/task-flow/backend/.env.development) về `"file:./dev.db"`.
- [x] Sửa lỗi cô lập môi trường test: Cấu hình `env` (DATABASE_URL và NODE_ENV) trực tiếp trong [vitest.config.ts](file:///c:/project/task-flow/backend/vitest.config.ts) để Vitest tiêm biến môi trường trước khi khởi tạo Prisma Client, thay vì gán thủ công trong [setup.ts](file:///c:/project/task-flow/backend/src/tests/setup.ts) (tránh lỗi import hoisting làm sạch nhầm cơ sở dữ liệu phát triển `dev.db`).
- [x] Khôi phục tệp cấu hình kiểm thử [setup.ts](file:///c:/project/task-flow/backend/src/tests/setup.ts) về sử dụng cơ sở dữ liệu tệp SQLite tạm thời `test.db` và tự động làm sạch sau khi hoàn thành.
- [x] Chạy lại `npx prisma generate` và gieo dữ liệu thành công (`npx prisma db seed`) trên SQLite.
- [x] Thực thi bộ kiểm thử tự động xác nhận 100% test cases của frontend (12/12) và backend (15/15) đều vượt qua thành công với cơ sở dữ liệu SQLite cục bộ và không làm ảnh hưởng đến dữ liệu phát triển ở `dev.db`.

### [x] Triển khai Mô hình API Key cá nhân (Bring Your Own Key - BYOK)
- [x] Thêm giao diện cấu hình API Key cá nhân trong [Settings.tsx](file:///c:/project/task-flow/frontend/src/pages/Settings.tsx) bằng một Card "Cấu hình trợ lý AI cá nhân".
- [x] Bổ sung tính năng kiểm tra kết nối với API Key, lưu trữ an toàn trong `localStorage` và xóa cấu hình trực tiếp từ giao diện cài đặt.
- [x] Cấu hình [MarkdownEditor.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownEditor.tsx) chuyển đổi nguồn lấy API Key sang `localStorage` của người dùng, hiển thị thông báo lỗi bằng `message.error` và chặn thao tác gọi AI nếu chưa cấu hình key cá nhân.
- [x] Chạy kiểm tra biên dịch (`npx.cmd tsc -b` trong `frontend/`) và chạy bộ unit tests thành công 100% để đảm bảo không lỗi Runtime.

### [x] Triển khai Bình luận phản hồi lồng nhau (Threaded Comments & Replies)
- [x] Cập nhật Prisma Schema thêm liên kết tự quy chiếu `parentId` cho bảng `Comment` và đồng bộ database thành công.
- [x] Cập nhật Backend controllers (`commentController.ts` và `taskController.ts`) hỗ trợ nhận `parentId`, ép phẳng (flatten) reply lồng sâu xuống tối đa 2 cấp bảo vệ di động và thay đổi thứ tự sắp xếp thời gian tăng dần (`createdAt: 'asc'`).
- [x] Tích hợp gửi thông báo realtime `COMMENT_ADDED` cho cả tác giả của bình luận gốc khi có phản hồi con.
- [x] Cập nhật API và React Query mutation `addCommentMutation` ở Frontend để truyền thêm `parentId`.
- [x] Xây dựng UI phản hồi lồng nhau trong [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) với khoảng đệm thụt lề `ml-6 sm:ml-8` và đường kẻ dọc liên kết.
- [x] Triển khai Form nhập phản hồi con Inline dưới mỗi comment và cơ chế thu gọn/mở rộng (Expand/Collapse) khi có nhiều hơn 2 phản hồi.
- [x] Khắc phục lỗi bảo mật & logic: chặn hoàn toàn việc người dùng tự phản hồi bình luận của chính mình cả ở Backend controller [commentController.ts](file:///c:/project/task-flow/backend/src/controllers/commentController.ts) (trả về lỗi validation 400) và ẩn nút "Trả lời" trên giao diện [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx).
- [x] Khắc phục lỗi đồng bộ thời gian thực (Real-time synchronization bug): Tích hợp phát sự kiện `comment:deleted` từ backend [commentController.ts](file:///c:/project/task-flow/backend/src/controllers/commentController.ts) và bổ sung listener lắng nghe sự kiện `comment:deleted` & sửa `comment:added` để invalidate chính xác query `['comments', taskId]` trên frontend [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx).
- [x] Biên dịch TypeScript (`frontend/` & `backend/`) và chạy bộ tests tự động PASS 100% (bao gồm cả test case mới bổ sung cho tính năng chặn tự phản hồi).

### [x] Bảo vệ Toàn vẹn Dữ liệu Cơ sở dữ liệu khi Cập nhật & Deploy (Database Safety & Seeding Protection)
- [x] Khắc phục lỗ hổng xóa dữ liệu cũ trong [seed.ts](file:///c:/project/task-flow/backend/prisma/seed.ts): loại bỏ hoàn toàn các câu lệnh `deleteMany()` phá hủy dữ liệu.
- [x] Cập nhật logic seed sử dụng `upsert` riêng biệt cho từng tài khoản mặc định để tránh ghi đè/xóa dữ liệu cũ của người dùng.
- [x] Loại bỏ khai báo biến chưa sử dụng (`admin`, `task3`, `task4`) gây lỗi biên dịch TypeScript trong [seed.ts](file:///c:/project/task-flow/backend/prisma/seed.ts).
- [x] Thực thi thành công gieo dữ liệu `prisma db seed` để khởi tạo đầy đủ các tài khoản mặc định (`admin@taskflow.com`, `leader@taskflow.com`, `member@taskflow.com`) trên cơ sở dữ liệu local.
- [x] Xóa bỏ tệp kiểm tra cơ sở dữ liệu tạm thời [check-db.ts](file:///c:/project/task-flow/backend/prisma/check-db.ts).
- [x] Tắt tiến trình chạy ngầm chiếm dụng cổng `5000` (EADDRINUSE 5000) để trả lại cổng cho người dùng khởi chạy.
- [x] Tích hợp cơ chế tự động sao lưu SQLite database (`dev.db`) kèm nhãn thời gian trước khi chạy `db push` trong tệp cấu hình CI/CD [.github/workflows/deploy.yml](file:///c:/project/task-flow/.github/workflows/deploy.yml) để đảm bảo an toàn dữ liệu trên production server.




