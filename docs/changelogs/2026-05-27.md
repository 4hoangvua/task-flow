# Changelog – 2026-05-27

### [x] Trình soạn thảo Markdown & Slash Commands (`/`)
- [x] Tạo mới thành phần dùng chung [MarkdownRenderer.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownRenderer.tsx) xử lý chuyển đổi văn bản Markdown thô (hỗ trợ Bold, Italic, Links, Unordered lists, Numbered lists, Dividers, Checklists `- [ ]`, và Khối mã ` ``` `) thành giao diện HTML Obsidian cao cấp.
- [x] Tạo mới thành phần soạn thảo [MarkdownEditor.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownEditor.tsx) tích hợp:
  - Thanh công cụ định dạng Bold, Italic, Link, Checklist, Lists, Code, H1, H2.
  - Phím tắt Lệnh nhanh Slash Commands (`/` trigger) mở popover lựa chọn định dạng và thay thế ký tự thông minh tại vị trí con trỏ chuột.
  - Chế độ xem trước (Preview) trực quan đồng bộ màu sắc Obsidian Zinc/Slate.
  - **Tối ưu di động**: Cấu hình flex-wrap và layout flex-col trên mobile giúp các phím toolbar tự động xuống dòng và nút Trợ lý AI co dãn đầy chiều rộng, chống tràn và ẩn công cụ trên điện thoại.

### [x] Tích hợp Trợ lý AI Soạn thảo (Google Gemini 1.5 Flash)
- [x] Tách cấu hình API Key: Đưa khóa API mặc định và biến môi trường cấu hình `VITE_GEMINI_API_KEY` vào các tệp môi trường [frontend/.env](file:///c:/project/task-flow/frontend/.env) và [frontend/.env.production](file:///c:/project/task-flow/frontend/.env.production), không lưu trữ cứng trong mã nguồn giao diện để tuân thủ tiêu chuẩn an toàn bảo mật.
- [x] **Cơ chế Xoay vòng & Tránh lỗi Rate Limit (Model Rotation & Fallback)**:
  - Định nghĩa danh sách xoay vòng các mô hình AI: `gemini-flash-latest`, `gemini-3.1-flash-lite`, `gemini-2.5-flash-lite`, `gemini-1.5-flash`, `gemini-1.5-flash-8b`, và `gemini-1.5-pro`.
  - Triển khai vòng lặp tự động chuyển tiếp: Nếu mô hình chính bị lỗi hoặc vượt quá số lượt dùng (HTTP 429), hệ thống tự động ghi nhận cảnh báo và lập tức gọi thử mô hình kế tiếp, giúp tránh gián đoạn trải nghiệm người dùng.
- [x] Hỗ trợ tự động tạo bản nháp mô tả đầy đủ cấu trúc (Mục tiêu, Các bước thực hiện checklist, Tiêu chí nghiệm thu) dựa trên **Tiêu đề công việc** hiện có, hoặc dựa trên **Prompt gợi ý tùy chỉnh** nhập vào từ người dùng.

### [x] Tích hợp Form & Chi tiết công việc
- [x] Cập nhật [TaskFormModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskFormModal.tsx) thay thế trường `Input.TextArea` của mô tả bằng trình soạn thảo `MarkdownEditor`. Sử dụng `Form.useWatch` để đồng bộ tiêu đề công việc phục vụ làm tham số đầu vào cho AI.
- [x] Cập nhật [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) thay thế hiển thị văn bản thô bằng component `MarkdownRenderer` giúp hiển thị nội dung mô tả công việc giàu định dạng.
- [x] **Checklist tương tác trực tiếp**: Nâng cấp [MarkdownRenderer.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownRenderer.tsx) và [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) cho phép người dùng click trực tiếp vào checkbox của Markdown trong mô tả. Khi click, hệ thống tự động tìm và thay thế trạng thái `- [ ]` / `- [x]` tương ứng ở dòng đó trong chuỗi mô tả thô và gọi API `updateTask` để đồng bộ database thời gian thực.
- [x] **Trình thu gọn mô tả dài**: Bọc mô tả Markdown trong một container có giới hạn chiều cao `max-h-36`, có lớp phủ mờ (fade-out gradient) ở dưới đáy khi thu gọn và hỗ trợ nút **"Xem chi tiết" / "Thu gọn"** giúp Modal không bị quá dài trên các thiết bị, tối ưu hóa giao diện di động.

### [x] Kiểm thử biên dịch
- [x] Chạy biên dịch TypeScript tĩnh (`npx tsc -b`) đạt kết quả thành công 100% không cảnh báo/lỗi kiểu dữ liệu.
- [x] Ghi nhận nhật ký kiểm thử functional test trong [2026-05-27.md](file:///c:/project/task-flow/docs/testing/2026-05-27.md) đạt PASS 100% cho toàn bộ 7 kịch bản thử nghiệm.
