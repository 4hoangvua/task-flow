# Changelog – 2026-05-27

### [x] Trình soạn thảo Markdown & Slash Commands (`/`)
- [x] Tạo mới thành phần dùng chung [MarkdownRenderer.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownRenderer.tsx) xử lý chuyển đổi văn bản Markdown thô (hỗ trợ Bold, Italic, Links, Unordered lists, Numbered lists, Dividers, Checklists `- [ ]`, và Khối mã ` ``` `) thành giao diện HTML Obsidian cao cấp.
- [x] Tạo mới thành phần soạn thảo [MarkdownEditor.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownEditor.tsx) tích hợp:
  - Thanh công cụ định dạng Bold, Italic, Link, Checklist, Lists, Code, H1, H2.
  - Phím tắt Lệnh nhanh Slash Commands (`/` trigger) mở popover lựa chọn định dạng và thay thế ký tự thông minh tại vị trí con trỏ chuột.
  - Chế độ xem trước (Preview) trực quan đồng bộ màu sắc Obsidian Zinc/Slate.
  - **Tối ưu di động**: Cấu hình flex-wrap và layout flex-col trên mobile giúp các phím toolbar tự động xuống dòng và nút Trợ lý AI co dãn đầy chiều rộng, chống tràn và ẩn công cụ trên điện thoại.

### [x] Tích hợp Trợ lý AI Soạn thảo (Google Gemini 1.5 Flash)
- [x] Tách cấu hình API Key: Đưa khóa API mặc định và biến môi trường cấu hình `VITE_GEMINI_API_KEY` vào các tệp môi trường [frontend/.env](file:///c:/project/task-flow/frontend/.env) và [frontend/.env.production](file:///c:/project/task-flow/frontend/.env.production), không lưu trữ cứng trong mã nguồn giao diện để tuân thủ tiêu chuẩn an toàn bảo mật.
- [x] **Cơ chế Xoay vòng & Tránh lỗi Rate Limit (Model Rotation & Fallback)**:
  - Định nghĩa danh sách xoay vòng các mô hình AI: `gemini-flash-latest`, `gemini-3.1-flash-lite`, `gemini-2.5-flash-lite`, `gemini-1.5-flash`, `gemini-1.5-flash-8b`, và `gemini-1.5-pro`.
  - Triển khai vòng lặp tự động chuyển tiếp: Nếu mô hình chính bị lỗi hoặc vượt quá số lượt dùng (HTTP 429), hệ thống tự động ghi nhận cảnh báo và lập tức gọi thử mô hình kế tiếp, giúp tránh gián đoạn trải nghiệm người dùng.
- [x] Hỗ trợ tự động tạo bản nháp mô tả đầy đủ cấu trúc (Mục tiêu, Các bước thực hiện checklist, Tiêu chí nghiệm thu) dựa trên **Tiêu đề công việc** hiện có, hoặc dựa trên **Prompt gợi ý tùy chỉnh** nhập vào từ người dùng.

### [x] Tích hợp Form & Chi tiết công việc
- [x] Cập nhật [TaskFormModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskFormModal.tsx) thay thế trường `Input.TextArea` của mô tả bằng trình soạn thảo `MarkdownEditor`. Sử dụng `Form.useWatch` để đồng bộ tiêu đề công việc phục vụ làm tham số đầu vào cho AI.
- [x] Cập nhật [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) thay thế hiển thị văn bản thô bằng component `MarkdownRenderer` giúp hiển thị nội dung mô tả công việc giàu định dạng.
- [x] **Checklist tương tác trực tiếp**: Nâng cấp [MarkdownRenderer.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownRenderer.tsx) và [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) cho phép người dùng click trực tiếp vào checkbox của Markdown trong mô tả. Khi click, hệ thống tự động tìm và thay thế trạng thái `- [ ]` / `- [x]` tương ứng ở dòng đó trong chuỗi mô tả thô và gọi API `updateTask` để đồng bộ database thời gian thực.
- [x] **Trình thu gọn mô tả dài**: Bọc mô tả Markdown trong một container có giới hạn chiều cao `max-h-36`, có lớp phủ mờ (fade-out gradient) ở dưới đáy khi thu gọn và hỗ trợ nút **"Xem chi tiết" / "Thu gọn"** giúp Modal không bị quá dài trên các thiết bị, tối ưu hóa giao diện di động.

### [x] Lịch công việc dự án (Task Calendar View)
- [x] Tạo mới thành phần [TaskCalendar.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskCalendar.tsx) hiển thị lịch tháng với lưới 42 ngày (tính từ Thứ 2).
- [x] Triển khai bộ lọc trên thanh công cụ Lịch: người dùng có thể lọc công việc theo Thành viên thực hiện, Độ ưu tiên và Trạng thái.
- [x] Xây dựng bảng thống kê (Quick Stats) ở sidebar trái (Tổng số việc, Việc khẩn cấp, Đang làm, Quá hạn, Hoàn thành) tính riêng theo từng tháng hiển thị, tích hợp tính năng click vào thẻ thống kê để lọc nhanh công việc trên lịch.
- [x] Tích hợp mở nhanh modal chi tiết [TaskDetailModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskDetailModal.tsx) khi click vào badge công việc trên lịch.
- [x] Hỗ trợ tạo công việc nhanh: khi rê chuột vào ô ngày, icon "+" sẽ hiển thị đối với Project LEADER/ADMIN, click vào sẽ mở [TaskFormModal.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskFormModal.tsx) với trường hạn chót (deadline) được tự động điền sẵn ngày đó. MEMBER bình thường bị ẩn icon này và bị chặn tạo.
- [x] Tích hợp tab Lịch công việc vào [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) giữa tab Bảng công việc và Thành viên.
- [x] Tự động đồng bộ hóa lịch theo thời gian thực khi có thay đổi deadline hoặc tạo mới task qua WebSocket room events.

### [x] Thiết lập & Triển khai Bộ kiểm thử tự động (Frontend & Backend Testing)
- [x] **Backend Integration Tests**:
  - Cài đặt `vitest`, `supertest`, `@types/supertest`.
  - Tạo mới tệp cấu hình [vitest.config.ts](file:///c:/project/task-flow/backend/vitest.config.ts) thiết lập môi trường `node` và cấu hình chạy tuần tự các test files (`fileParallelism: false`) nhằm tránh xung đột SQLite database locked. Cập nhật thêm cấu hình `include: ['src/tests/**/*.test.ts']` để loại bỏ các tệp JavaScript CommonJS đã biên dịch trong thư mục `dist/`.
  - Tạo mới tệp thiết lập môi trường test [setup.ts](file:///c:/project/task-flow/backend/src/tests/setup.ts) tự động override `DATABASE_URL` trỏ tới `prisma/test.db`, đồng bộ hóa schema trước test suite (`npx prisma db push --force-reset`) và dọn dẹp file db sau test suite.
  - Sửa đổi [index.ts](file:///c:/project/task-flow/backend/src/index.ts) bỏ qua listen cổng HTTP và start deadline scheduler nếu ở môi trường test (nhận diện qua `process.env.VITEST`).
  - Viết tệp kiểm thử [auth.test.ts](file:///c:/project/task-flow/backend/src/tests/auth.test.ts) kiểm thử register, login, me profile, và update details. Tích hợp dọn dẹp toàn diện cơ sở dữ liệu trước mỗi ca test để tránh lỗi foreign key.
  - Viết tệp kiểm thử [project.test.ts](file:///c:/project/task-flow/backend/src/tests/project.test.ts) kiểm thử CRUD dự án, member role management. Tích hợp dọn dẹp toàn diện cơ sở dữ liệu trước mỗi ca test.
  - Viết tệp kiểm thử [task.test.ts](file:///c:/project/task-flow/backend/src/tests/task.test.ts) kiểm thử CRUD task, dependencies block, circular dependencies block (DFS), subtasks management. Tích hợp dọn dẹp toàn diện cơ sở dữ liệu trước mỗi ca test.
  - Thêm script `test` chạy `vitest run` vào [package.json](file:///c:/project/task-flow/backend/package.json).
  - Toàn bộ backend tests chạy đạt kết quả **PASS 100% (15/15 tests)**.
- [x] **Frontend Unit & Component Tests**:
  - Cài đặt `vitest`, `@testing-library/react`, `@testing-library/dom`, `@testing-library/jest-dom`, `jsdom`.
  - Tạo mới tệp cấu hình [vitest.config.ts](file:///c:/project/task-flow/frontend/vitest.config.ts) thiết lập môi trường `jsdom`.
  - Tạo mới tệp setup test [setupTests.ts](file:///c:/project/task-flow/frontend/src/setupTests.ts) thiết lập DOM mocks cho `window.matchMedia`, `socket.io-client`, và Recharts.
  - Viết tệp kiểm thử [PriorityTag.test.tsx](file:///c:/project/task-flow/frontend/src/components/common/PriorityTag.test.tsx) kiểm thử hiển thị nhãn dịch tiếng Việt và style tương ứng theo độ ưu tiên.
  - Viết tệp kiểm thử [MarkdownRenderer.test.tsx](file:///c:/project/task-flow/frontend/src/components/common/MarkdownRenderer.test.tsx) kiểm thử render heading, text formats, gom nhóm lists (LF/JSX gotcha fixed), và checklist callback.
  - Viết tệp kiểm thử [TaskCalendar.test.tsx](file:///c:/project/task-flow/frontend/src/components/task/TaskCalendar.test.tsx) kiểm thử render header, filter placeholder regex, task badges và open detail modal trigger.
  - Thêm script `test` chạy `vitest run` vào [package.json](file:///c:/project/task-flow/frontend/package.json).
  - Toàn bộ frontend tests chạy đạt kết quả **PASS 100% (12/12 tests)**.

### [x] Nâng cấp cơ chế đồng bộ hóa Socket.io & Tự phục hồi kết nối
- [x] Tích hợp bộ lắng nghe lỗi kết nối `connect_error` tại [SocketProvider.tsx](file:///c:/project/task-flow/frontend/src/providers/SocketProvider.tsx). Khi gặp lỗi xác thực, hệ thống tự động gọi API `/api/auth/refresh` bằng Refresh Token để lấy Access Token mới, cập nhật `authStore` và kết nối lại Socket mà không làm gián đoạn trải nghiệm người dùng. Nếu Refresh Token hết hạn, tự động đăng xuất người dùng.
- [x] Tích hợp cơ chế tự động tái gia nhập (rejoin) phòng dự án tại [ProjectDetail.tsx](file:///c:/project/task-flow/frontend/src/pages/ProjectDetail.tsx) bằng cách lắng nghe sự kiện `connect` trên Socket.io client. Khi Socket.io mất mạng và kết nối lại thành công, client tự động gửi sự kiện `join-project` lên server để khôi phục trạng thái đồng bộ bảng Kanban và lịch thời gian thực.

### [x] Kiểm thử biên dịch & Chức năng
- [x] Chạy biên dịch TypeScript tĩnh (`npx tsc -b` ở frontend và `npx tsc` ở backend) đạt kết quả thành công 100% không cảnh báo/lỗi kiểu dữ liệu.
- [x] Ghi nhận nhật ký kiểm thử functional test trong [2026-05-27.md](file:///c:/project/task-flow/docs/testing/2026-05-27.md) đạt PASS 100% cho toàn bộ kịch bản thử nghiệm (cả manual functional và automated testing).

### [x] Khắc phục lỗi cấu hình cơ sở dữ liệu ở môi trường phát triển (Development DB Configuration Fix)
- [x] Sửa đổi tệp cấu hình môi trường [backend/.env.development](file:///c:/project/task-flow/backend/.env.development) để cập nhật `DATABASE_URL` từ PostgreSQL (`postgresql://...`) về SQLite (`file:./dev.db`), đồng nhất môi trường cục bộ và ngăn chặn hoàn toàn lỗi crash runtime của Prisma Client (`Error validating datasource 'db': the URL must start with the protocol 'file:'`) khi chạy tiến trình lập lịch (scheduler) hoặc thực hiện đăng nhập.

### [x] Khắc phục lỗi xung đột lockfile khi chạy CI/CD (CI/CD Lockfile Conflict Fix)
- [x] Cập nhật cấu hình CI/CD trong [.github/workflows/deploy.yml](file:///c:/project/task-flow/.github/workflows/deploy.yml) thêm lệnh `git reset --hard` trước khi chạy `git pull origin main` tại cả thư mục Frontend và Backend trên máy chủ AWS, giúp tự động xóa bỏ các thay đổi cục bộ phát sinh ngoài ý muốn đối với tệp `package-lock.json` sau mỗi lần chạy `npm install` và đảm bảo tiến trình kéo mã nguồn mới diễn ra thành công không bị gián đoạn.


